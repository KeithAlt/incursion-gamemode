-- %r will be replaced with the reason. %e will be replaced with when the ban expires.
jBans.BanFormat = "\nYou have been banned.\nReason: %r\nBan expires: %e"
-- This will be the ban reason if none is specified
jBans.DefaultBanMessage = "No reason"
-- Should we kick the player if we fail to connect to the database when attempting to check if they're banned?
jBans.KickOnFailure = true
